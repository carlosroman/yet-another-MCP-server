[project]
name = "yams"
version = "0.1.0"
license = "MIT"
license-files = ["LICENSE"]
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
test = []
lint = [
    "ruff",
]
dev = [
    { include-group = "test" },
    { include-group = "lint" },
]